apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static-s3-bucket
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  volumeName: new-pv